{
  "build": {
    "env": {
      "API_URL": "$api_url",
      "ASGARDEO_CLIENT_ID": "$client_id",
      "ASGARDEO_BASE_URL": "$base_url",
      "VERCEL_URL": "$vercel_url"
    }
  },
  "buildCommand": "npm run build && sed -i 's/API_URL/'\"$API_URL\"'/g' dist/*/main*.js && sed -i 's/VERCEL_URL/'\"$VERCEL_URL\"'/g' dist/*/main*.js && sed -i 's/ASGARDEO_CLIENT_ID/'\"$ASGARDEO_CLIENT_ID\"'/g' dist/*/main*.js && sed -i 's/ASGARDEO_BASE_URL/'\"$ASGARDEO_BASE_URL\"'/g' dist/*/main*.js",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
